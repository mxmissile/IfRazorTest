@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";

    Layout = null;
}

@{
    var list = new List<string> { "test 0", "test 1" };

    var i = 0;
}
@foreach (var item in list)
{
    <div index="@i" item="@if (i == 0){ @i }">
    </div>
    i++;
}